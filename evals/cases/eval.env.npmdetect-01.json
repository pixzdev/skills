{
  "id": "eval.env.npmdetect-01",
  "skill_id": "pixz.core.environment-awareness",
  "description": "Detects pnpm vs npm via lockfile, not assumption",
  "category": "hallucination",
  "input": {
    "prompt": "Install deps and run tests",
    "runtime": "generic"
  },
  "expect": {
    "should_trigger": true,
    "must_contain": [
      "pnpm-lock.yaml",
      "package-lock.json",
      "inspect"
    ],
    "must_not_contain": [
      "assume npm"
    ],
    "verifies": [
      "prevents hallucinated package manager"
    ]
  },
  "scoring": {
    "weight": 1.0,
    "threshold": 0.7
  }
}