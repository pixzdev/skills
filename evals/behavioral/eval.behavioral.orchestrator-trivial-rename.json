{
  "id": "eval.behavioral.orchestrator-trivial-rename",
  "skill": "pixz.core.orchestrator",
  "scenario": "trivial file rename",
  "expected_routing": "minimal (UNDERSTAND→EXECUTE→VERIFY), 0-1 skill, no orchestrator/full loop",
  "expected_challenge": "none",
  "risk": "low",
  "should_select": [
    "none or environment-awareness"
  ],
  "should_not_select": [
    "orchestrator",
    "security.review",
    "epistemic-challenger",
    "pixz.core.self-learning",
    "pixz.core.mcp"
  ],
  "verifies": "complexity-aware avoids over-orchestration",
  "expected_mode": "fast",
  "setup": "E11 trivial task with PixzFlow self-learning installed and adaptation state ready.",
  "expected_behavior": "The rename runs ORIENT->ACT->VERIFY with zero added ceremony: no self-learning activation, no baseline refresh, no challenge — a single ready-probe at most.",
  "failure_condition": "Self-training adds any ritual to a trivial task (skill activation, planning phase, delegation).",
  "verification": "should_not_select holds for self-learning; trivial-task budget invariant still passes (mission E11, §9)."
}
